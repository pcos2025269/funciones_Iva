
/** Esta funcion debuelve el resultado del total de la suma del subtotal y el IVA
 * sacado de la funcion del calculo del IVA asi devolviendo la suma de estos 2 siendo 
 * el total de la compra  
*/
export function calcularTotal(subtotal: number, iva: number): number {
    return subtotal + iva;
}

